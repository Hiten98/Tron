# Tron
Surge Protector
for (int i = 0; i < gameBoard.length; i++) {
            for (int j = 0; j < gameBoard.length; j++) {
                if(gameBoard[i][j] == myPlayerNumber)
                {
                    myCurrentRow = i;
                    myCurrentColumn = j;
                }
            }
        }
